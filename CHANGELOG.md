# Changelog

## Unreleased

- M3 skeleton: `vcfcf_migrator` package on `vcf-cf-tooling-core` 0.1.0,
  `version`, `inspect`, `ui`; `tree`, `build`, `corpus-check` stubbed.
- CI on pull requests and pushes to main; release on `vX.Y.Z` tags with a
  Linux, macOS and Windows binary plus the wheel.
